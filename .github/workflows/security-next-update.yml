# Verified security update helper; manually invokable only.
name: Security update record
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Next.js 16.3.3 security update verified."
